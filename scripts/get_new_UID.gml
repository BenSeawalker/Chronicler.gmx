return ++global.GUID;

/*
var n_guid = 1;
var found = true;

while(found)
{
    found = false;
    for(var i=0;i<ds_list_size(allbubbles) && !found;i++)
    {
        if(allbubbles[|i].UID == n_guid)
            found = true;
    }
        
    if(found)
        n_guid++;
}

return n_guid;
*/
